{
  "name": "neuraforgeos-monorepo-container",
  "version": "0.1.0",
  "private": true,
  "description": "Container directory for NeuraForge OS monorepo - main project is in neuraforge-os/",
  "scripts": {
    "dev": "cd neuraforge-os && bun run dev",
    "build": "cd neuraforge-os && bun run build",
    "install": "cd neuraforge-os && bun install"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "turbo": "^2.5.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}